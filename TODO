[general]
  * add command line options
  * add some helper functions for lua
    - split string
    - pack & unpack
    - ...
  * add build stuff
  * add template system
[new plugins]
  * mpd
  * uptime
  * wlan
  * countdown
[plugin improvements]
  * clock
      -> configurable format
      -> support different languages
  * loadavg
      -> configurable format
  * updates
      -> configurable format
      -> configurable updatelog-file
