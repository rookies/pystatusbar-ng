[general]
  * add command line options
  * add some helper functions for lua
    - split string
    - pack & unpack
    - ...
  * add template system
[new plugins]
  * mpd
  * uptime
  * wlan
  * countdown
[plugin improvements]
  * clock
      -> configurable format
  * loadavg
      -> configurable format
  * updates
      -> configurable format
  * cputemp
      -> configurable format
  * countdown
      -> configurable format
